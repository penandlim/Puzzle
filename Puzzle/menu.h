#include<SFML/Graphics.hpp>
#include<SFML/Audio.hpp>

int menu(sf::RenderWindow & Window, sf::VertexArray & backgroundVertex);
int colorchange(int color);

extern sf::Music intro_piano;
extern bool applaunch;